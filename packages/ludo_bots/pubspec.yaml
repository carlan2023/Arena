name: ludo_bots
description: Easy and normal Ludo bots on top of ludo_engine. Pure Dart.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  ludo_engine:
    path: ../ludo_engine
dev_dependencies:
  coverage: ^1.15.1
  lints: ^6.1.0
  test: ^1.32.0
