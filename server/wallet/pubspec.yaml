name: arena_wallet
description: Wallet for the Arena server. Double entry ledger, payment providers and the payments service.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  arena_auth:
    path: ../auth
  crypto: ^3.0.7
  http: ^1.6.0
  postgres: ^3.5.17

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
