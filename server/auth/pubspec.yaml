name: arena_auth
description: Login for the Arena server. Token verifiers, session tokens and the user store.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  asn1lib: ^1.6.5
  crypto: ^3.0.7
  http: ^1.6.0
  pointycastle: ^4.0.0
  postgres: ^3.5.17

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
